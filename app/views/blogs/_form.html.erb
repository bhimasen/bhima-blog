<%= bootstrap_form_for(@blog) do |f| %>
  <%= f.text_field :title %>
  <%= f.text_field :sub_title %>
  <%= f.text_area :description, :rows => 10, style: 'width:100%;' %>
  <%= f.submit "Create" %>
<% end %>